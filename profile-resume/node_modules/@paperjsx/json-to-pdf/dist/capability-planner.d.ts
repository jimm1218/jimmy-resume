import type { PdfDocument } from "./engine.js";
import type { PdfRenderOptions } from "./phase9-types.js";
export interface PdfCapabilities {
    assets: boolean;
    encryption: boolean;
    flatPages: boolean;
    forms: boolean;
    interactive: boolean;
    layout: boolean;
    pdfa: boolean;
    signature: boolean;
    streaming: boolean;
    tables: boolean;
    taggedAccessibility: boolean;
}
export type PdfSelectedPhase = "phase2-flat" | "phase3-layout" | "phase5-tables" | "phase6-interactive" | "phase7-tagged" | "phase8-pdfa";
export interface PdfCapabilityPlan {
    capabilities: PdfCapabilities;
    passes: string[];
    selectedPhase: PdfSelectedPhase;
}
export declare function analyzePdfCapabilities(document: PdfDocument, options?: PdfRenderOptions): PdfCapabilities;
export declare function planPdfCapabilities(document: PdfDocument, options?: PdfRenderOptions): PdfCapabilityPlan;
//# sourceMappingURL=capability-planner.d.ts.map