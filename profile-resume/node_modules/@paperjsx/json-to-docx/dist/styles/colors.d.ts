/**
 * Color conversion utilities for DOCX.
 *
 * DOCX uses 6-digit hex colors without the # prefix.
 * This module handles conversion from CSS colors.
 */
/**
 * Check if a CSS color is transparent.
 */
export declare function isTransparentColor(color: string | undefined): boolean;
/**
 * Convert CSS color to DOCX color format (6-digit hex without #).
 * Returns undefined for transparent colors.
 */
export declare function cssColorToDocx(color: string | undefined): string | undefined;
/**
 * Get shading object from a CSS background color.
 * Returns undefined if the color is transparent or invalid.
 */
export declare function getShadingFromColor(cssColor: string | undefined): {
    fill: string;
    type: 'solid';
} | undefined;
/**
 * Get a contrasting text color (black or white) for a background.
 */
export declare function getContrastingTextColor(backgroundColor: string | undefined): string;
/**
 * Lighten a color by a percentage.
 * @param color - Hex color (with or without #)
 * @param percent - Percentage to lighten (0-100)
 */
export declare function lightenColor(color: string, percent: number): string;
/**
 * Darken a color by a percentage.
 * @param color - Hex color (with or without #)
 * @param percent - Percentage to darken (0-100)
 */
export declare function darkenColor(color: string, percent: number): string;
//# sourceMappingURL=colors.d.ts.map