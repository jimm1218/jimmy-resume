/**
 * DOCX Style Definition Builder
 *
 * Builds DOCX style definitions that generate proper OOXML output.
 * Generates styles.xml content for Word documents.
 */
export interface DocxStyle {
    styleId: string;
    name: string;
    type: 'paragraph' | 'character' | 'table' | 'numbering';
    basedOn?: string;
    next?: string;
    isDefault?: boolean;
    paragraph?: ParagraphProperties;
    run?: RunProperties;
}
export interface ParagraphProperties {
    alignment?: 'left' | 'center' | 'right' | 'both';
    spacing?: {
        before?: number;
        after?: number;
        line?: number;
        lineRule?: 'auto' | 'exact' | 'atLeast';
    };
    indent?: {
        left?: number;
        right?: number;
        firstLine?: number;
        hanging?: number;
    };
    outlineLevel?: number;
    keepNext?: boolean;
    keepLines?: boolean;
}
export interface RunProperties {
    fontFamily?: string;
    fontSize?: number;
    bold?: boolean;
    italic?: boolean;
    underline?: string;
    strike?: boolean;
    color?: string;
    highlight?: string;
    vertAlign?: 'superscript' | 'subscript';
}
/**
 * Generate default document styles.
 */
export declare function generateDefaultStyles(): DocxStyle[];
/**
 * Generate styles.xml content.
 */
export declare function generateStylesXml(styles: DocxStyle[]): string;
/**
 * Create a custom style based on an existing one.
 */
export declare function createCustomStyle(baseStyle: DocxStyle, overrides: Partial<DocxStyle>): DocxStyle;
/**
 * Get a style by ID from a collection.
 */
export declare function getStyleById(styles: DocxStyle[], styleId: string): DocxStyle | undefined;
/**
 * Merge style collections, with later collections overriding earlier ones.
 */
export declare function mergeStyles(...collections: DocxStyle[][]): DocxStyle[];
export interface StyleTheme {
    primaryColor: string;
    secondaryColor: string;
    headingFont: string;
    bodyFont: string;
    codeFont: string;
    baseFontSize: number;
}
/**
 * Default style theme (Office-like).
 */
export declare const DEFAULT_THEME: StyleTheme;
/**
 * Generate styles based on a theme.
 */
export declare function generateThemedStyles(theme: StyleTheme): DocxStyle[];
//# sourceMappingURL=style-builder.d.ts.map