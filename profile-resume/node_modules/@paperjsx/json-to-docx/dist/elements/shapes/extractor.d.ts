/**
 * Shape Extractor for DOCX
 * ========================
 * Utilities for extracting and processing shape data from ShapeElement.
 */
import type { ShapeElement, ShapeType, TextRun } from '../../types';
/**
 * Extracted shape data in a normalized format.
 */
export interface ExtractedShape {
    /** Element ID */
    id: string;
    /** Shape type */
    shapeType: ShapeType;
    /** Position and dimensions */
    bounds: {
        x: number;
        y: number;
        width: number;
        height: number;
    };
    /** Fill style (normalized) */
    fill?: NormalizedFill;
    /** Stroke style (normalized) */
    stroke?: NormalizedStroke;
    /** Text content */
    text?: string;
    /** Text runs for formatted content */
    runs?: TextRun[];
    /** Whether shape contains text */
    hasText: boolean;
    /** Custom path data (for custom shapes) */
    pathData?: string;
    /** Computed metrics */
    metrics: ShapeMetrics;
}
/**
 * Normalized fill style.
 */
export interface NormalizedFill {
    type: 'solid' | 'gradient' | 'none';
    color?: string;
    opacity?: number;
}
/**
 * Normalized stroke style.
 */
export interface NormalizedStroke {
    width: number;
    color: string;
    style: 'solid' | 'dashed' | 'dotted';
}
/**
 * Computed shape metrics.
 */
export interface ShapeMetrics {
    /** Area in square pixels */
    area: number;
    /** Perimeter in pixels (approximated for complex shapes) */
    perimeter: number;
    /** Aspect ratio (width / height) */
    aspectRatio: number;
    /** Whether shape is approximately square */
    isSquare: boolean;
    /** Whether shape is oriented horizontally */
    isHorizontal: boolean;
    /** Center point */
    center: {
        x: number;
        y: number;
    };
}
/**
 * Extract and normalize shape data from a ShapeElement.
 */
export declare function extractShape(element: ShapeElement): ExtractedShape;
/**
 * Check if shape type is a geometric primitive.
 */
export declare function isPrimitiveShape(shapeType: ShapeType): boolean;
/**
 * Check if shape type is a polygon.
 */
export declare function isPolygonShape(shapeType: ShapeType): boolean;
/**
 * Check if shape typically contains text.
 */
export declare function isTextContainerShape(shapeType: ShapeType): boolean;
/**
 * Get number of sides for polygon shapes.
 */
export declare function getPolygonSides(shapeType: ShapeType): number | null;
/**
 * Validate a shape element.
 */
export declare function validateShape(element: ShapeElement): string[];
/**
 * Extract plain text from shape.
 */
export declare function getShapeText(element: ShapeElement): string;
/**
 * Get text length in shape.
 */
export declare function getShapeTextLength(element: ShapeElement): number;
//# sourceMappingURL=extractor.d.ts.map