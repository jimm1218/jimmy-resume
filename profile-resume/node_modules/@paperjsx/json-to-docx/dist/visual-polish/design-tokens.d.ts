/**
 * Design Token System (Doc 1, Section 2)
 * ======================================
 *
 * Implements the SOTA Token Schema for enterprise-grade theming.
 * Supports print-safe units (pt, mm) and ensures layout consistency
 * regardless of screen resolution.
 */
import { Theme, ThemeTokens, ColorTokens, SpacingTokens, TypographyTokens, HexColor, RGBColor, PT } from "./types";
/** Default enterprise light theme */
export declare const DEFAULT_THEME: Theme;
/** Enterprise dark theme */
export declare const DARK_THEME: Theme;
/** Compact theme (9pt font) */
export declare const COMPACT_THEME: Theme;
/** Spacious theme (12pt font) */
export declare const SPACIOUS_THEME: Theme;
/** Theme registry for quick lookup */
export declare const THEME_REGISTRY: Map<string, Theme>;
/**
 * Parse a PT value from string (e.g., "12pt" -> 12)
 */
export declare function parsePT(value: string): PT;
/**
 * Format a PT value to string (e.g., 12 -> "12pt")
 */
export declare function formatPT(value: PT): string;
/**
 * Convert hex color to RGB
 */
export declare function hexToRgb(hex: HexColor): RGBColor;
/**
 * Convert RGB to hex color
 */
export declare function rgbToHex(rgb: RGBColor): HexColor;
/**
 * Calculate relative luminance for contrast checking
 * (WCAG 2.1 formula)
 */
export declare function calculateLuminance(rgb: RGBColor): number;
/**
 * Calculate contrast ratio between two colors
 * (WCAG 2.1 formula)
 */
export declare function calculateContrastRatio(color1: HexColor, color2: HexColor): number;
/**
 * Check if contrast meets WCAG AA standard (4.5:1 for normal text)
 */
export declare function meetsWCAGAA(color1: HexColor, color2: HexColor): boolean;
/**
 * Check if contrast meets WCAG AAA standard (7:1 for normal text)
 */
export declare function meetsWCAGAAA(color1: HexColor, color2: HexColor): boolean;
/**
 * Calculate type scale sizes based on ratio
 */
export declare function calculateTypeScale(baseSize: PT, ratio: number, steps?: number): PT[];
/**
 * DesignTokenManager - Manages theme tokens and provides computed values
 */
export declare class DesignTokenManager {
    private theme;
    private typeScale;
    constructor(theme?: Theme);
    /**
     * Get the current theme
     */
    getTheme(): Theme;
    /**
     * Set a new theme
     */
    setTheme(theme: Theme): void;
    /**
     * Load theme by ID from registry
     */
    loadTheme(themeId: string): boolean;
    /**
     * Get a color token
     */
    getColor(key: keyof ColorTokens): HexColor | HexColor[] | undefined;
    /**
     * Get a spacing token as PT value
     */
    getSpacing(key: keyof SpacingTokens): PT | undefined;
    /**
     * Get a typography token
     */
    getTypography<K extends keyof TypographyTokens>(key: K): TypographyTokens[K] | undefined;
    /**
     * Get the grid base unit
     */
    getGridBase(): PT;
    /**
     * Get the base font size
     */
    getBaseFontSize(): PT;
    /**
     * Get computed type scale
     */
    getTypeScale(): PT[];
    /**
     * Get font size for heading level
     */
    getHeadingSize(level: 1 | 2 | 3 | 4 | 5 | 6): PT;
    /**
     * Get chart color at index (wraps around)
     */
    getChartColor(index: number): HexColor;
    /**
     * Check contrast between text and background
     */
    checkTextContrast(): {
        passes: boolean;
        ratio: number;
    };
    /**
     * Compute type scale from base size and ratio
     */
    private computeTypeScale;
    /**
     * Merge partial theme tokens
     */
    mergeTokens(partialTokens: Partial<ThemeTokens>): void;
    /**
     * Export tokens as JSON
     */
    toJSON(): string;
    /**
     * Create from JSON
     */
    static fromJSON(json: string): DesignTokenManager;
}
/** Default design token manager instance */
export declare const designTokenManager: DesignTokenManager;
//# sourceMappingURL=design-tokens.d.ts.map