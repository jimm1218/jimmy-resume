import type { DocxFeatureId } from './pro-guard/feature-ids.js';
export declare const IS_PRO: boolean;
export type { DocxFeatureId };
export { FEATURE_IDS, isDocxFeatureId, resolveDocxFeatureId } from './pro-guard/feature-ids.js';
/**
 * Package-level licence assertion. Takes a human-readable API method
 * name, not a feature id — these fire at public API entry points
 * ("renderToDocx", "hydrateDocx", …) to ensure a licence key is
 * present before any work happens.
 */
export declare function assertDocxPackageLicense(apiName: string, licenseKey?: string): void;
/**
 * Feature-level pro gate. `feature` must be a canonical `DocxFeatureId`
 * from `src/pro-guard/feature-ids.ts`. Legacy strings are remapped but
 * new call sites must use the canonical form.
 */
export declare function requireDocxPro(feature: DocxFeatureId | string, licenseKey?: string): void;
//# sourceMappingURL=pro-guard.d.ts.map