import type { DocxDocument } from '../schema.js';
export interface ContractParty {
    name: string;
    address: string;
    /** e.g. "Licensor", "Client". */
    role: string;
}
export interface ContractClause {
    /** e.g. "1", "2.1". */
    number: string;
    title: string;
    content: string;
    subclauses?: Array<{
        /** e.g. "a", "i". */
        label: string;
        content: string;
    }>;
}
export interface ContractSignature {
    name: string;
    title: string;
    /** Which party this signatory represents. */
    party: string;
}
export interface BuildContractDocxInput {
    title: string;
    /** Free-form date string. */
    effectiveDate: string;
    parties: ContractParty[];
    recitals?: string[];
    clauses: ContractClause[];
    signatures?: ContractSignature[];
    theme?: 'corporate' | 'classic' | 'academic';
    pageSize?: 'a4' | 'letter' | 'legal';
}
export declare function buildContractDocx(input: BuildContractDocxInput): DocxDocument;
//# sourceMappingURL=contract.d.ts.map