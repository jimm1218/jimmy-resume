/**
 * High-level invoice builder.
 *
 * Mirrors the `generate_invoice_docx` MCP wrapper shape so the documented
 * Mode B examples work without rewriting. See
 * docs/0428-claude-test-based-directive2.md §"@paperjsx/json-to-docx".
 */
import type { DocxDocument } from '../schema.js';
export interface InvoiceParty {
    name: string;
    address: string;
    email?: string;
    phone?: string;
    taxId?: string;
}
export interface InvoiceLineItem {
    description: string;
    quantity: number;
    unitPrice: number;
    amount: number;
}
export interface BuildInvoiceDocxInput {
    invoiceNumber: string;
    date: string;
    dueDate: string;
    sender: InvoiceParty;
    recipient: InvoiceParty;
    items: InvoiceLineItem[];
    subtotal: number;
    /** Tax rate as decimal (e.g. 0.0875 for 8.75%). */
    taxRate?: number;
    taxAmount: number;
    total: number;
    /** ISO 4217 currency code. Defaults to 'USD'. */
    currency?: string;
    /** Notes appended after totals. */
    notes?: string;
    theme?: 'corporate' | 'modern' | 'minimal';
    pageSize?: 'a4' | 'letter';
}
export declare function buildInvoiceDocx(input: BuildInvoiceDocxInput): DocxDocument;
//# sourceMappingURL=invoice.d.ts.map