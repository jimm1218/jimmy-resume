/**
 * VLT Invariant Validator
 * =======================
 * Validates that VLT structure meets required invariants before serialization.
 * This catches layout bugs that would result in visually broken output.
 */
import type { PolyglotDocument, PolyglotPage } from './types';
export interface ValidationIssue {
    severity: 'error' | 'warning';
    code: string;
    message: string;
    nodeType?: string;
    nodePath?: string;
    details?: Record<string, unknown>;
}
export interface ValidationResult {
    valid: boolean;
    issues: ValidationIssue[];
    stats: {
        nodesChecked: number;
        errorsFound: number;
        warningsFound: number;
    };
}
/**
 * Validate a single page's VLT structure
 */
export declare function validatePage(page: PolyglotPage, pageIndex: number): ValidationResult;
/**
 * Validate entire VLT document
 */
export declare function validateVLT(doc: PolyglotDocument): ValidationResult;
/**
 * Validate VLT and throw if invalid (use before serialization)
 */
export declare function assertVLTValid(doc: PolyglotDocument): void;
/**
 * Log validation results (for debugging)
 */
export declare function logValidationResult(result: ValidationResult): void;
//# sourceMappingURL=vlt-validator.d.ts.map