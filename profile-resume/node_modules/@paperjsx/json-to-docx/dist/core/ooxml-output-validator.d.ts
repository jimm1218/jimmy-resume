export interface OoxmlValidationIssue {
    severity: 'error' | 'warning';
    code: 'DOCX_TAB_NEGATIVE' | 'DOCX_CONTENT_TYPES_OVERRIDE_MISSING' | 'DOCX_RELATIONSHIP_TARGET_MISSING' | 'DOCX_VALIDATOR_INTERNAL';
    message: string;
    part?: string;
    details?: Record<string, unknown>;
}
export interface OoxmlValidationResult {
    ok: boolean;
    issues: OoxmlValidationIssue[];
}
/**
 * Validate a rendered DOCX buffer against the strict-mode invariants.
 * Pure read-only — does not mutate the buffer.
 */
export declare function validateDocxBuffer(buffer: Buffer | Uint8Array): Promise<OoxmlValidationResult>;
export declare class DocxStrictValidationError extends Error {
    readonly issues: OoxmlValidationIssue[];
    constructor(issues: OoxmlValidationIssue[]);
}
//# sourceMappingURL=ooxml-output-validator.d.ts.map