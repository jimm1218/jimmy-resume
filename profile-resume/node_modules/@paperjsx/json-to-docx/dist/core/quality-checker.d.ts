/**
 * Quality Checker
 * ===============
 * Validates PolyglotDocument structure and content quality.
 * Returns a quality score and detailed check results.
 */
import type { PolyglotDocument } from './types';
export type QualitySeverity = 'critical' | 'major' | 'minor';
export interface QualityCheck {
    /** Unique check ID */
    id: string;
    /** Human-readable check name */
    name: string;
    /** Severity level */
    severity: QualitySeverity;
    /** Check function */
    check: (doc: PolyglotDocument) => QualityResult;
}
export interface QualityResult {
    /** Whether the check passed */
    passed: boolean;
    /** Failure message (if not passed) */
    message?: string;
    /** Additional details */
    details?: unknown;
}
export interface QualityCheckResult {
    check: QualityCheck;
    result: QualityResult;
}
export interface QualityReport {
    /** Overall quality score (0-100) */
    score: number;
    /** Whether the document passes minimum quality threshold */
    passed: boolean;
    /** Number of critical failures */
    criticalFailures: number;
    /** Number of major failures */
    majorFailures: number;
    /** Number of minor failures */
    minorFailures: number;
    /** Individual check results */
    results: QualityCheckResult[];
    /** Summary of issues */
    issues: string[];
}
export declare const QUALITY_CHECKS: QualityCheck[];
/**
 * Run all quality checks on a document
 */
export declare function runQualityChecks(doc: PolyglotDocument, checks?: QualityCheck[]): QualityReport;
/**
 * Quick check if a document meets minimum quality
 */
export declare function isDocumentValid(doc: PolyglotDocument): boolean;
/**
 * Get a summary string of quality issues
 */
export declare function getQualitySummary(doc: PolyglotDocument): string;
//# sourceMappingURL=quality-checker.d.ts.map