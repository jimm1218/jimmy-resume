/**
 * Color Utilities
 * ===============
 * Robust color parsing and conversion for PPTX/DOCX output.
 * Handles various CSS color formats and converts to 6-digit hex.
 */
/**
 * Parse a CSS color string to 6-digit hex (without #)
 * Returns undefined if the color cannot be parsed
 */
export declare function parseColor(color: string | undefined | null): string | undefined;
/**
 * Parse a color with a fallback value
 */
export declare function parseColorWithFallback(color: string | undefined | null, fallback?: string): string;
/**
 * Calculate relative luminance per WCAG 2.1
 * Input: 6-digit hex string (no #)
 */
export declare function relativeLuminance(hex: string): number;
/**
 * Calculate contrast ratio between two colors per WCAG 2.1
 * Input: 6-digit hex strings (no #)
 */
export declare function contrastRatio(hex1: string, hex2: string): number;
/**
 * Check if a string is a valid color
 */
export declare function isValidColor(color: string | undefined | null): boolean;
/**
 * Get list of supported named colors
 */
export declare function getSupportedNamedColors(): string[];
//# sourceMappingURL=color-utils.d.ts.map