/**
 * @paperjsx/polyglot-core - Coordinates Module
 * =============================================
 *
 * This module provides the coordinate transformation system for Polyglot.
 *
 * Key Concepts:
 * - NormalizedRect: Coordinates as fractions (0-1) of page dimensions
 * - Transform Pipeline: VLT → Normalized → Target Format (PPTX/DOCX)
 * - Validators: Catch invalid coordinates before they become visual bugs
 *
 * Usage:
 * ```typescript
 * import {
 *   vltToNormalized,
 *   normalizedToPPTXInches,
 *   validateNormalized,
 * } from '@paperjsx/polyglot-core';
 *
 * // Transform VLT rect to normalized
 * const normalized = vltToNormalized(rect, pageDimensions);
 *
 * // Validate before conversion
 * const validation = validateNormalized(normalized);
 * if (!validation.valid) {
 *   throw new Error(validation.errors.map(e => e.message).join(', '));
 * }
 *
 * // Convert to target format
 * const pptxInches = normalizedToPPTXInches(normalized, 10, 5.625);
 * ```
 */
export type { NormalizedRect, FrozenNormalizedRect, CoordinateErrorCode, CoordinateSeverity, CoordinateError, CoordinateValidation, } from './normalized-rect';
export { createNormalizedRect, createFrozenNormalizedRect, createNormalizedRectUnsafe, getRight, getBottom, getCenterX, getCenterY, getArea, rectsOverlap, rectContains, rectContainsPoint, rectIntersection, boundingBox, rectsEqual, rectsHorizontallyAdjacent, rectsVerticallyAdjacent, scaleRect, translateRect, insetRect, expandRect, clampRect, toPlainObject, fromPlainObject, formatRect, formatRectAsPercent, } from './normalized-rect';
export type { VLTToNormalizedConfig, PPTXRect, DOCXRect, } from './transform-pipeline';
export { PIXELS_PER_INCH, TWIPS_PER_INCH, EMU_PER_INCH, DEFAULT_SLIDE_WIDTH_INCHES, DEFAULT_SLIDE_HEIGHT_INCHES, DEFAULT_PAGE_WIDTH_INCHES, DEFAULT_PAGE_HEIGHT_INCHES, vltToNormalized, vltToNormalizedBatch, normalizedToVLT, normalizedToPPTXInches, normalizedToEMU, normalizedToDOCXTwips, vltToPPTXInches, vltToEMU, vltToDOCXTwips, pxToNormalizedWidth, pxToNormalizedHeight, normalizedWidthToInches, normalizedHeightToInches, pxToInches, inchesToPx, pxToTwips, twipsToPx, inchesToEMU, emuToInches, verifyRoundTrip, createPipeline, } from './transform-pipeline';
export type { ValidationOptions, TableInvariantResult, DocumentValidationResult, } from './validators';
export { validateNormalized, assertNormalizedValid, validateVLTRect, validateTableInvariants, assertTableInvariants, validateDocumentCoordinates, assertDocumentCoordinatesValid, validateColumnWidths, validateRowHeights, } from './validators';
//# sourceMappingURL=index.d.ts.map