/**
 * Input Sanitization
 * ==================
 * Functions to sanitize and validate input data for safe document generation.
 */
import type { PolyglotNode, Rect } from './types';
/**
 * Sanitize text content by removing null bytes and limiting length
 */
export declare function sanitizeTextContent(text: string | undefined | null): string;
/**
 * Check if a string contains potentially problematic characters
 */
export declare function hasProblematicCharacters(text: string): boolean;
/**
 * Sanitize a Rect by clamping values to safe ranges
 */
export declare function sanitizeRect(rect: Rect | undefined): Rect;
/**
 * Sanitize a PolyglotNode tree, fixing issues and limiting depth
 */
export declare function sanitizeNode(node: PolyglotNode, depth?: number): PolyglotNode;
/**
 * Safely divide two numbers, returning a fallback on division by zero
 */
export declare function safeDivide(numerator: number, denominator: number, fallback?: number): number;
/**
 * Clamp a number to a range
 */
export declare function clamp(value: number, min: number, max: number): number;
/**
 * Ensure a value is a valid positive number
 */
export declare function ensurePositive(value: number, fallback?: number): number;
export declare const SANITIZER_LIMITS: {
    MAX_TEXT_LENGTH: number;
    MAX_NODE_DEPTH: number;
    MAX_X: number;
    MAX_Y: number;
    MAX_DIMENSION: number;
};
//# sourceMappingURL=sanitizer.d.ts.map