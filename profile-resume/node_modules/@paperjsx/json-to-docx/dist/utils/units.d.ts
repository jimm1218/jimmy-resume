/**
 * Unit conversion utilities for DOCX generation.
 *
 * DOCX uses several different unit systems:
 * - Twips (twentieths of a point): 1 inch = 1440 twips
 * - Half-points: Used for font sizes
 * - EMUs (English Metric Units): Used for images (914400 EMUs = 1 inch)
 * - DXA: Used for table widths (same as twips)
 *
 * # Branded output types (Phase 1.1)
 *
 * Every conversion function returns a **branded number** — a nominal
 * type that carries its unit in its type. Branded numbers are still
 * assignable to plain `number`, so legacy callers that store results
 * in `number` variables keep working without change. Consumers that
 * opt into branded parameter types (e.g. `Twips` instead of `number`)
 * get compile-time protection against unit confusion:
 *
 *   function writeWidth(w: Twips) { ... }
 *   writeWidth(pointsToHalfPoints(12));   // TYPE ERROR — HalfPoints ≠ Twips
 *   writeWidth(pxToTwips(800));           // OK
 *
 * Escape hatches: use `asTwips`, `asHalfPoints`, etc. when you are
 * passing a value that is *known* to already be in the target unit
 * (e.g. reading from an OOXML XML attribute). Document the reason.
 */
declare const __unitBrand: unique symbol;
export type Twips = number & {
    readonly [__unitBrand]: 'twips';
};
export type HalfPoints = number & {
    readonly [__unitBrand]: 'halfpoints';
};
export type EMU = number & {
    readonly [__unitBrand]: 'emu';
};
export type Points = number & {
    readonly [__unitBrand]: 'points';
};
export type Px = number & {
    readonly [__unitBrand]: 'px';
};
export type Inches = number & {
    readonly [__unitBrand]: 'inches';
};
export type Mm = number & {
    readonly [__unitBrand]: 'mm';
};
export type LineSpacingDxa = number & {
    readonly [__unitBrand]: 'line-dxa';
};
/** Escape hatches — use when you already know the unit of a raw number. */
export declare const asTwips: (n: number) => Twips;
export declare const asHalfPoints: (n: number) => HalfPoints;
export declare const asEmu: (n: number) => EMU;
export declare const asPoints: (n: number) => Points;
export declare const asPx: (n: number) => Px;
export declare const asInches: (n: number) => Inches;
export declare const asMm: (n: number) => Mm;
export declare const asLineSpacingDxa: (n: number) => LineSpacingDxa;
export declare const DPI = 96;
export declare const TWIPS_PER_INCH = 1440;
export declare const TWIPS_PER_POINT = 20;
export declare const EMU_PER_INCH = 914400;
export declare const EMU_PER_PIXEL = 9525;
/**
 * Page size presets in twips.
 */
export declare const PAGE_SIZES: {
    readonly LETTER: {
        readonly width: Twips;
        readonly height: Twips;
    };
    readonly A4: {
        readonly width: Twips;
        readonly height: Twips;
    };
    readonly LEGAL: {
        readonly width: Twips;
        readonly height: Twips;
    };
    readonly TABLOID: {
        readonly width: Twips;
        readonly height: Twips;
    };
    readonly A3: {
        readonly width: Twips;
        readonly height: Twips;
    };
    readonly A5: {
        readonly width: Twips;
        readonly height: Twips;
    };
};
/**
 * Convert CSS pixels to twips.
 */
export declare function pxToTwips(px: number): Twips;
export declare function twipsToPx(twips: number): Px;
/**
 * Convert CSS pixels to points (for font sizes).
 */
export declare function pxToPoints(px: number): Points;
export declare function pointsToPx(pt: number): Px;
/**
 * Convert CSS pixels to half-points (DOCX font size unit).
 */
export declare function pxToHalfPoints(px: number): HalfPoints;
export declare function halfPointsToPx(hp: number): Px;
export declare function pointsToHalfPoints(pt: number): HalfPoints;
export declare function halfPointsToPoints(hp: number): Points;
export declare function inchesToTwips(inches: number): Twips;
export declare function twipsToInches(twips: number): Inches;
export declare function mmToTwips(mm: number): Twips;
export declare function twipsToMm(twips: number): Mm;
export declare function pxToEmu(px: number): EMU;
export declare function emuToPx(emu: number): Px;
export declare function inchesToEmu(inches: number): EMU;
/**
 * Convert line height multiplier to DOCX line spacing value (DXA-style).
 * DOCX line spacing 240 = single spacing (1.0)
 */
export declare function lineHeightToDocx(multiplier: number): LineSpacingDxa;
/**
 * Parse a CSS size value and convert to twips.
 * Handles px, pt, em, rem, in, mm, cm units.
 */
export declare function cssSizeToTwips(value: string | number | undefined, baseFontSize?: number): Twips;
/**
 * Parse a CSS font size value and convert to half-points.
 */
export declare function cssFontSizeToHalfPoints(value: string | number | undefined, baseFontSize?: number): HalfPoints;
export {};
//# sourceMappingURL=units.d.ts.map