import type { Change, DiffStatistics } from "@paperjsx/document-diff";
import { type TrackChangesGranularity } from "../core/revision-tracker.js";
export interface CompareDocumentsOptions {
    author?: string;
    date?: string;
    granularity?: TrackChangesGranularity;
    licenseKey?: string;
}
export interface CompareDocumentsResult {
    buffer: Buffer;
    changes: Change[];
    summary: string;
    statistics: DiffStatistics;
}
export declare function compareDocuments(originalBuffer: Buffer, revisedBuffer: Buffer, options?: CompareDocumentsOptions): Promise<CompareDocumentsResult>;
//# sourceMappingURL=compare-documents.d.ts.map