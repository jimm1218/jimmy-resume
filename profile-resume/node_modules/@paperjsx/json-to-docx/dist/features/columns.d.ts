/**
 * Multi-column layout support for DOCX.
 *
 * Enables 2-4 column layouts in documents.
 */
import type { StructuredElement } from '../types';
/**
 * Extended style interface for column detection.
 */
interface ExtendedStyle {
    display?: string;
    columnCount?: number | string;
    [key: string]: unknown;
}
/**
 * Column configuration.
 */
export interface ColumnConfig {
    count: number;
    space: number;
    separate?: boolean;
    equalWidth?: boolean;
    widths?: number[];
}
/**
 * Default column spacing (0.5 inch).
 */
export declare const DEFAULT_COLUMN_SPACE: import("..").Twips;
/**
 * Create column configuration.
 */
export declare function createColumnConfig(count: number, options?: {
    space?: number;
    separate?: boolean;
    widths?: number[];
}): ColumnConfig;
/**
 * Detect column count from CSS styles.
 */
export declare function detectColumnsFromStyle(style: ExtendedStyle): number;
/**
 * Detect columns from element children (flex/grid layouts).
 */
export declare function detectColumnsFromChildren(children: StructuredElement[]): number;
/**
 * Convert column config to DOCX section properties.
 * Returns basic column configuration (custom widths not supported in this simplified version).
 */
export declare function columnConfigToDocx(config: ColumnConfig): {
    column: {
        count: number;
        space: number;
        separate: boolean | undefined;
    };
} | undefined;
/**
 * Calculate equal column widths.
 */
export declare function calculateEqualColumnWidths(contentWidth: number, // twips
columnCount: number, gapWidth?: number): number[];
/**
 * Create a two-column layout configuration.
 */
export declare function createTwoColumnConfig(options?: {
    space?: number;
    separate?: boolean;
}): ColumnConfig;
/**
 * Create a three-column layout configuration.
 */
export declare function createThreeColumnConfig(options?: {
    space?: number;
    separate?: boolean;
}): ColumnConfig;
/**
 * Create a left-narrow, right-wide layout (sidebar style).
 */
export declare function createSidebarLayout(contentWidth: number, sidebarWidth?: number, space?: number): ColumnConfig;
/**
 * Create a newspaper-style layout (multiple equal columns).
 */
export declare function createNewspaperLayout(columns?: number, space?: number): ColumnConfig;
/**
 * Determine if content should be in columns based on element type.
 */
export declare function shouldUseColumns(elementType: string): boolean;
export {};
//# sourceMappingURL=columns.d.ts.map