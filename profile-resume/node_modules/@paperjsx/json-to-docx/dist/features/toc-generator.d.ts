/**
 * Table of Contents Generator for DOCX (Raw OOXML)
 *
 * Generates Table of Contents using raw OOXML format.
 * Creates proper field codes that Word can update.
 */
import { SemanticElement } from '../semantic-types';
export interface TocEntry {
    text: string;
    level: number;
    bookmark: string;
}
export interface TocOptions {
    title?: string;
    levels?: number;
    showPageNumbers?: boolean;
    hyperlinks?: boolean;
}
/**
 * Extract TOC entries from semantic elements.
 */
export declare function extractTocEntries(elements: SemanticElement[], maxLevel?: number): TocEntry[];
/**
 * Generate TOC field code XML.
 */
export declare function generateTocXml(options?: TocOptions): string;
/**
 * Generate TOC styles to add to styles.xml.
 */
export declare function generateTocStyles(): string;
/**
 * Generate bookmark for heading (used for TOC hyperlinks).
 */
export declare function generateHeadingBookmark(_headingContent: string, bookmarkId: number, bookmarkName: string): {
    start: string;
    end: string;
};
/**
 * Generate a heading with a TOC bookmark.
 */
export declare function generateHeadingWithBookmark(content: string, level: number, bookmarkId: number, bookmarkName: string): string;
/**
 * Generate a manual TOC entry (for custom TOCs).
 */
export declare function generateManualTocEntry(text: string, pageNumber: number, level?: number): string;
/**
 * Generate a complete manual TOC from entries.
 */
export declare function generateManualToc(entries: TocEntry[], title?: string): string;
/**
 * Generate List of Figures field.
 */
export declare function generateListOfFigures(title?: string): string;
/**
 * Generate List of Tables field.
 */
export declare function generateListOfTables(title?: string): string;
//# sourceMappingURL=toc-generator.d.ts.map