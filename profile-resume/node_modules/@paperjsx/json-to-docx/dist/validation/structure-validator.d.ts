/**
 * Structure Validator for DOCX
 *
 * Validates document structure for DOCX compatibility.
 * Detects issues that could cause problems in Word.
 */
import { SemanticElement } from '../semantic-types';
export interface ValidationResult {
    valid: boolean;
    warnings: ValidationWarning[];
    errors: ValidationError[];
}
export interface ValidationWarning {
    element: SemanticElement;
    message: string;
    suggestion: string;
}
export interface ValidationError {
    element: SemanticElement;
    message: string;
}
/**
 * Validates document structure for DOCX compatibility.
 */
export declare function validateStructure(elements: SemanticElement[]): ValidationResult;
/**
 * Validate heading hierarchy for proper TOC generation.
 */
export declare function validateHeadingHierarchy(elements: SemanticElement[]): {
    valid: boolean;
    issues: string[];
};
/**
 * Validate list structure.
 */
export declare function validateListStructure(elements: SemanticElement[]): {
    valid: boolean;
    issues: string[];
};
/**
 * Validate table structure for DOCX compatibility.
 */
export declare function validateTableStructure(elements: SemanticElement[]): {
    valid: boolean;
    issues: string[];
};
/**
 * Validate image references.
 */
export declare function validateImages(elements: SemanticElement[]): {
    valid: boolean;
    issues: string[];
};
/**
 * Run all validators and return combined results.
 */
export declare function validateAll(elements: SemanticElement[]): {
    structure: ValidationResult;
    headings: {
        valid: boolean;
        issues: string[];
    };
    lists: {
        valid: boolean;
        issues: string[];
    };
    tables: {
        valid: boolean;
        issues: string[];
    };
    images: {
        valid: boolean;
        issues: string[];
    };
    overallValid: boolean;
};
/**
 * Get a summary report of validation results.
 */
export declare function getValidationSummary(elements: SemanticElement[]): string;
//# sourceMappingURL=structure-validator.d.ts.map