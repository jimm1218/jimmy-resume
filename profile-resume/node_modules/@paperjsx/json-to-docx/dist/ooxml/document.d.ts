import type { StructuredDocument } from '../types.js';
import type { SerializationContext } from './context.js';
import { type ElementSerializerOptions } from './element-serializers.js';
export interface NativeDocumentBuildOptions extends ElementSerializerOptions {
    autoNoProof?: boolean;
    columns?: number;
    watermark?: string | {
        text?: string;
        opacity?: number;
        rotation?: number;
    };
    /**
     * Cancellation signal checked at each page boundary. Throws the
     * signal's abort reason (or a generic DOMException) on cancellation.
     */
    signal?: AbortSignal;
    /**
     * Fires once per page after the page's elements have been serialized.
     */
    onProgress?: (progress: {
        phase: 'serializing';
        percent: number;
        pageIndex: number;
        pageCount: number;
        message?: string;
    }) => void;
}
interface SectionPartReference {
    type: 'default' | 'first' | 'even';
    relationshipId: string;
}
export interface NativeSectionReferences {
    headerReferences: SectionPartReference[];
    footerReferences: SectionPartReference[];
    titlePage: boolean;
    columns?: number;
}
export declare function buildDocumentXml(document: StructuredDocument, context: SerializationContext, options?: NativeDocumentBuildOptions): Promise<string>;
export {};
//# sourceMappingURL=document.d.ts.map