import type { ListElement } from '../types.js';
import { type ExtractedListItem } from '../elements/lists/extractor.js';
import type { DeterministicContext } from './deterministic.js';
export interface RegisteredList {
    numId: number;
    items: ExtractedListItem[];
}
export declare class NumberingRegistry {
    private readonly deterministic;
    private readonly definitions;
    private abstractNumId;
    private numId;
    constructor(deterministic: DeterministicContext, startAbstractNumId?: number, startNumId?: number);
    registerList(element: ListElement): RegisteredList;
    hasDefinitions(): boolean;
    toXml(): string;
}
//# sourceMappingURL=numbering.d.ts.map